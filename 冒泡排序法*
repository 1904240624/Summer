/*将N个整数按从小到大排序的冒泡排序法是这样工作的：从头到尾比较相邻两个元素，如果前面的元素大于其紧随的后面元素，则交换它们。通过一遍扫描，则最后一个元素必定是最大的元素。然后用同样的方法对前N−1个元素进行第二遍扫描。依此类推，最后只需处理两个元素，就完成了对N个数的排序。

本题要求对任意给定的K（<N），输出扫描完第K遍后的中间结果数列。*/
#include<stdio.h>
int main()
{
    int N,K,a[100],t;
    scanf("%d%d",&N,&K);
    for(int i=0;i<N;i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=1;i<N;i++)      //冒泡排序法
    {
        for(int j=0;j<N-i;j++)
        {
            if(a[j]>a[j+1])
            {
                t=a[j];
                a[j]=a[j+1];
                a[j+1]=t;
            }
        }
        K--;         //扫描K次后退出循环
        if(!K)
            break;
    }
    for(int i=0;i<N;i++)
    {
        if(i==N-1)    //当输出最后一个元素是不需要加空格
        {
            printf("%d",a[i]);
        }
        else
        printf("%d ",a[i]);
    }
}
